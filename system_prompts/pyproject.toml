[project]
name = "system-prompts-exercise"
version = "0.1.0"
description = "System prompts exercise from Anthropic Academy 'Building with the Claude API'"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.39.0",
]
